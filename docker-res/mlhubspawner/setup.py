from setuptools import setup

setup(name='mlhubspawner',
      version='0.1',
      description='MLHub DockerSpawner with multiple options to set',
      url='https://github.com/ml-tooling/ml-hub',
      author='MLTooling Team',
      packages=['mlhubspawner'],
      zip_safe=False)
